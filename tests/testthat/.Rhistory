setwd("../..")
Counts
historyCount
            current <- data.frame(date = as.character("2020-07-12"),
                                  rbind(c(attr$total.confirmed,
                                          0,
                                          attr$total.confirmed - attr$total.recovered - attr$total.deaths,
                                          attr$total.recovered,
                                          attr$total.deaths)))
current
historyCount[121,]
historyCount[121,6]=646
historyCount
current
current$date= "2020-07-13"
current
historyCount
            historyCount <- rbind(historyCount, current)

historyCount
Counts$counts
attributes(Counts$counts)
gsub('\\(updated |\\ EST)|/2020', '', attributes(Counts$counts)$update.time)
gsub('\\(updated |\\ EST)|/2020', ' ', attributes(Counts$counts)$update.time)
q()
n
